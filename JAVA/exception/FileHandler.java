package exception;

// 파일관련 작업을 담당하는 클래스
public class FileHandler {

    // src 파일을 dest 디렉토리로 복사하는 작업 처리
    public void copyFile(String src, String dest) {
        // 만약 파일이나 디렉토리가 존재하지 않으면 복사 할 수 없는데...

        /*
        boolean isExisted = false; // 파일이 존재하는지 여부

        if (!isExisted) {
            throw new CopyFailedException();

        } else {
            // 존재하면 복사 작업을 수행.....

        }
        */
    }
}

